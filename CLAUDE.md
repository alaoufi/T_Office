# T_Office — دليل المشروع (CLAUDE.md)

تطبيق مكتبي شخصي لنظام **أندرويد فقط** (APK)، يعمل **بلا إنترنت** بالكامل. بيانات المستخدم محلية على الجهاز.

## القرارات المعمارية الثابتة

| الموضوع | القرار |
|--------|--------|
| المنصة | Android فقط (APK) — لا يوجد موقع/خادم |
| اللغة | Kotlin |
| الواجهة | Jetpack Compose + Material 3 |
| الاتجاه | RTL عربي افتراضي |
| قاعدة البيانات | Room (SQLite) محلية — أوفلاين 100% |
| المعمارية | MVVM + طبقات (data / domain / ui) لكل وحدة |
| الحقن | Hilt (DI) |
| التنقل | Navigation Compose |
| الإشعارات/التذكير | WorkManager + Notifications + AlarmManager |
| الأمان | قفل PIN/بصمة (BiometricPrompt) + تشفير قاعدة (SQLCipher عند الحاجة) |
| الحد الأدنى | minSdk 26 (Android 8) — targetSdk/compileSdk 34 |

## الوحدات

1. **المهام (Tasks)** — أولويات، تواريخ استحقاق، تذكيرات، إنجاز.
2. **الملاحظات (Notes)** — نص، تصنيف، بحث.
3. **المواعيد/التقويم (Calendar)** — أحداث، تنبيهات.
4. **المصروفات/الميزانية (Expenses)** — دخل/مصروف، فئات، تقارير.
5. **المستندات/الملفات (Documents)** — حفظ وإرفاق ملفات/صور.
6. **محرر النصوص (Editor)** — محرر أصلي + استيراد/تصدير DOCX + عرض/تصدير PDF.
7. **الأمان (Security)** — قفل عند الفتح (PIN/بصمة).
8. **النسخ الاحتياطي (Backup)** — تصدير/استيراد ملف نسخة كامل.

## محرر النصوص — نطاق واقعي (مهم)

- **DOCX**: استيراد + تحرير + تصدير بتنسيق جيد جداً (فقرات، خط/حجم/لون، غامق/مائل/تسطير، محاذاة، قوائم، جداول بسيطة، صور). **ليس** تطابقاً 100% مع وورد للمستندات المعقدة جداً.
- **PDF**: عرض (PdfRenderer) + تصدير المستند إلى PDF. لا يوجد تحرير نص داخل PDF موجود.
- **DOC** (الصيغة الثنائية القديمة): غير مدعومة (دعمها ضعيف في كل المكتبات) — التركيز على DOCX.
- مكتبة DOCX المرشحة: استكشاف Apache POI ooxml أو بديل أخف متوافق مع أندرويد.

## مبادئ العمل (لتقليل المشاكل والمراجعات)

- وحدة واحدة لكل دفعة/مراجعة، صغيرة ومركّزة.
- كل تغيير يجب أن **يُبنى** بنجاح قبل الدفع: `./gradlew assembleDebug`.
- نموذج البيانات (Room) يُراجَع أولاً قبل بناء الواجهة.
- الفرع: `claude/arabic-office-app-f628j3`.

## أوامر البناء

```bash
export ANDROID_HOME=/opt/android-sdk
export ANDROID_SDK_ROOT=/opt/android-sdk
./gradlew assembleDebug      # ينتج app/build/outputs/apk/debug/app-debug.apk
./gradlew lint               # فحص جودة
```

## ترتيب التنفيذ

1. الهيكل: مشروع Gradle + Compose + Room + Hilt + Navigation + RTL + شاشة رئيسية (Dashboard).
2. الوحدات السهلة: المهام، الملاحظات، التقويم، المصروفات، المستندات، القفل، النسخ الاحتياطي.
3. APK يعمل ويُختبر.
4. محرر النصوص (DOCX/PDF) كمرحلة مستقلة.
